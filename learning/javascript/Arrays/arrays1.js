/* let marks = {
    stu1 : 97,
    stu2 : 82,
}

let marks1 = [20, 30, 40, 34];
console.log(marks1);
console.log(marks1.length);

let heroes = ["iron man", "thor", "hulk", "spider man"];
console.log(heroes) */

/* let heroes = ["iron man", "thor", "hulk", "spider man"];

//console.log(heroes[0])

for (let i = 0; i < heroes.length; i++){
    console.log(heroes[i]);
}

for (let hero of heroes) {
    console.log(hero);
} */


/* let marks = [10, 20, 30, 40, 50];

let sum = 0;

for (let val of marks) {
    sum += val;
}

let avg = sum/marks.length;
console.log(`avg marks of the of the class = ${avg}`);
 */

/* let items = [100, 200, 300, 400, 500];

let i = 0;
for (let val of items) {
   
    let offer = val/10;
    items[i] = items[i]-offer;
    console.log(`value after offer = ${items[i]}`);
    i++;
} */


/* let foodItems = ['Potato', 'Apple', 'Litchi', 'Tomato']
console.log(foodItems)

let deletedVal = foodItems.pop(); */

/* let companies = ["Bloomberg", "Microsoft", "Google", "Netflix", "IBM"]

console.log(companies)
companies.shift();

companies.splice(2,1,"Ola")

companies.push("Amazon")
 */

/* let x = null;
let y = typeof(x);
console.log(y)
 */


//object
let person = {
    name: "Alice",
    age: 30,
    isStudent: false,
};

//arrays
/* let fruits = ['Apple', 'Banana', 'Cherry']

for (let i = 0; i < fruits.length; i++) {
    console.log(fruits[i])
}

for (let fruit of fruits){
    console.log(fruit)
}
 */

let x= "abcde"
console.log(x.toLowerCase())